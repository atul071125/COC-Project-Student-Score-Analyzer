# Student Report Analyzer

A simple C program that analyzes student scores and provides an overview including the **average**, **maximum**, and **lowest** scores.  
This project demonstrates modular programming concepts in C by using functions for specific tasks like calculating averages and finding the highest/lowest scores.

---

## 📋 Features

- Accepts input for multiple student scores  
- Calculates:
  - Average score  
  - Highest score  
  - Lowest score  
- Displays a summary report of the results  

---

## 💻 Technologies Used

- **Language:** C  
- **Compiler:** GCC or any C compiler (e.g., Code::Blocks, Turbo C, clang)

---

## Program Logic

The project is divided into three main functions:

1. **`calculate_average()`** – Computes the average of all entered scores.  
2. **`find_max_score()`** – Finds the highest score among all students.  
3. **`find_lowest_score()`** – Finds the lowest score among all students.  

The `main()` function handles user input and displays the results.

---

## 🧩 How to Run the Program

### **Step 1:** Clone or download the project
```bash
git clone https://github.com/atul071125/Student-Score-Analyzer.git
cd Student-Score-Analyzer
